import type { Subscriber } from '../@types';
export declare const tokenizeDestination: (dest: string) => string[];
export declare const checkSubMatchDestination: (subscriber: Subscriber, dest: string) => boolean;
